# Searching for Files: Overview

Being able to quickly find the files you're looking for saves time and enhances productivity. Files can be searched for in the home directory or in any other location on the system.

The primary tools for this are the `locate` and `find` utilities. Wildcards in Bash are also available, allowing files matching a specific pattern to be specified rather than naming each one explicitly.
